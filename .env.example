# --- Google Cloud ---
# Option A: path-based ADC (Render: mount as secret file and set GOOGLE_APPLICATION_CREDENTIALS)
# GOOGLE_APPLICATION_CREDENTIALS=/etc/secrets/gcp.json
# Option B: inline JSON
# GOOGLE_CREDENTIALS={"type":"service_account",...}

# Optional: default bucket for GCS uploads
# GCS_BUCKET=podcast-tt

# --- Cloudflare R2 (preferred) ---
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
# R2_BUCKET=podcast-tt
# R2_PUBLIC_BASE_URL=https://pub-xxxxxxxxxx.r2.dev