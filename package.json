{
  "name": "tts-chunker-service",
  "version": "1.1.0",
  "type": "module",
  "main": "index.js",  // Changed from src/index.js
  "scripts": {
    "start": "node ./index.js",  // Changed path
    "dev": "nodemon ./index.js", // Changed path
    "build": "npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  // ... rest remains the same

  "dependencies": {
    "@aws-sdk/client-s3": "^3.669.0",
    "@aws-sdk/lib-storage": "^3.669.0",
    "@google-cloud/text-to-speech": "^5.8.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.1.3",
    "winston": "^3.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  
}
